type Query{
    userById(id:ID): User
}

type User{
    id:ID
    name:String
    age:Int
    gender: Gender
    accountInfo: AccountInfo
}

type AccountInfo{
    id:ID
    username : String
    password : String
    mobileNumber : String
}

enum Gender{
    MALE
    FEMALE
}
